{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nestjs",
  "installCommand": "pnpm install --prod=false",
  "buildCommand": "turbo run build && pnpm exec prisma migrate deploy && pnpm exec tsx prisma/seed.ts",
  "outputDirectory": "dist",
  "regions": ["fra1"]
}
